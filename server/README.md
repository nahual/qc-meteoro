# What?
Implementation of a weather service

# Running
You can either use foreman:
	foreman start

Or manually by:
	CONF=config/dev.py python speedracer.py

# Adding dependencies
 - edit requirements.txt to add your dependecy
 - run
    pip install -r requirements.txt
